pipeline {
    agent {
        label 'master' // Use your desired agent label here
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Helm Chart') {
            steps {
                sh 'helm install mk ./mychart/'
            }
        }
    }
}

